# ==============================================================================
# CONFIG AUTOGENERATED ON {{ ansible_date_time.iso8601 }}
# ==============================================================================

{% if coredns.private_zone is defined %}
{{ coredns.private_zone }} {
  hosts /etc/coredns/private_hosts
  log
  errors
}
{% endif %}

. {
  forward . {{ coredns.forward_to }}
  log
  errors
  cache
}